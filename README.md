# weather-data-pipeline
An end-to-end ETL pipeline that extracts raw weather data from the Open-Meteo API, transforms and cleans it, and loads it into a PostgreSQL database. This project demonstrates core data engineering concepts using Python and PostgreSQL for scalable, maintainable data pipelines.
